const getNotes = require('./notes.js');

const msg = getNotes();

console.log(msg);

/* challenge 1: see utils.js

Uncomment the lines below to execute the first challenge.
You will also need to uncommet the lines from utils.js
*/

// const add = require('./utils.js');
// const sum = add(4, -2);
// console.log(sum);
