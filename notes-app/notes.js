const getNotes = function () {
    return 'Your notes...';
}

// job is to save notes to data store
const addNote = function (title, body) {
    
}

// exports
module.exports = {
    getNotes: getNotes,
    addNote: addNote
}